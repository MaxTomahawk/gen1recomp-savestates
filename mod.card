return {
  summary = "Native rolling quicksaves, autosaves, permanent slots, and recovery checkpoints.",
  author = "MaxTomahawk",
  contact = "https://github.com/MaxTomahawk/gen1recomp-savestates",
  tags = { "quality-of-life", "ui", "tool" },
  differences = {
    changed = {
      "title CONTINUE can resume the newest valid normal save or savestate when enabled",
    },
    added = {
      "rolling quick and automatic checkpoints",
      "ten permanent state slots",
      "one-level undo-load recovery",
      "title browsing and deterministic battle safe-point restoration",
    },
    known = {
      "early access: final ROM-backed and physical-device acceptance is pending",
      "battle START-menu access awaits an engine release containing upstream PR #1077",
      "Party icons in state details await an engine release containing upstream PR #1079; text remains available",
      "only runtime boundaries proven safe by the engine are supported",
    },
  },
  credits = {
    { who = "Gen1Recomp contributors", for_ = "the native runtime and public mod API" },
  },
  compat = { engine = ">=0.1.79 <1.0.0", modApi = 2 },
}
